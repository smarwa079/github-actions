on:
    push:
        branches:
            - master



jobs:
    build:
        runs-on: ubuntu-latest
        
        steps:
            - name: 'Testing GitHub actions'
              run: pwd 

            - name: 'Testing Printing'
              run: echo ahmed